{
  "name": "exiledata-assets",
  "version": "0.5.3",
  "private": true,
  "description": "Static CDN of PoE2 item art (.webp) + skill videos (.webm). `npm start` serves the tree locally over HTTP on :4201 via wrangler, honoring _headers so local dev mirrors the deployed Cloudflare Pages site.",
  "scripts": {
    "start": "npx -y wrangler pages dev . --port 4201",
    "stage": "node scripts/stage-deploy.mjs",
    "deploy": "node --env-file-if-exists=.env scripts/deploy.mjs"
  }
}
